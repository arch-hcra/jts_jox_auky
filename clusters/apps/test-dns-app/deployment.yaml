apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-dns-app
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-dns-app
  template:
    metadata:
      labels:
        app: test-dns-app
    spec:
      containers:
        - name: test-dns-app
          image: nginx:alpine
          ports:
            - containerPort: 80
