apiVersion: v1
kind: Service
metadata:
  name: test-dns-app
  annotations:
    external-dns.alpha.kubernetes.io/hostname: test.my-test.local
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: test-dns-app
