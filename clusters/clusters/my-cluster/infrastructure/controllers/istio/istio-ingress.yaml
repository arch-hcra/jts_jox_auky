apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: istio-ingress
  namespace: flux-system
spec:
  interval: 5m
  targetNamespace: istio-system
  chart:
    spec:
      chart: gateway
      version: "1.21.2"
      sourceRef:
        kind: HelmRepository
        name: istio
        namespace: flux-system
  values: {}
